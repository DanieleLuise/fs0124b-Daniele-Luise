import { ColoreDirective } from './colore.directive';

describe('ColoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoreDirective();
    expect(directive).toBeTruthy();
  });
});
